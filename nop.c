#include "monty.h"
/**
  *v_nop- print no stack
  *@lead: pointing to the stack top
  *@bouncer: line of current numb
 */
void v_nop(stack_t **lead, unsigned int bouncer)
{
	(void) bouncer;
	(void) lead;
}
